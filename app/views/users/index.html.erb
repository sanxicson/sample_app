<% provide(:title, "Users") %>
<div class="center jumbotron">
	<h1><center><strong>USERS</strong></center></h1>
</div>

<%= will_paginate %>

<ul class="users" >
	<%= render @users %>
</ul>

<%= will_paginate %>